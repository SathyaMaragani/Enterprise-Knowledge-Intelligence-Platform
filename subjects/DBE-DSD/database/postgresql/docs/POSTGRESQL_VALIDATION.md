# PostgreSQL Validation Report

## Overview
- **Docker Image Used**: `postgres:16-alpine`
- **PostgreSQL Version**: `PostgreSQL 16.14 on x86_64-pc-linux-musl, compiled by gcc (Alpine 15.2.0) 15.2.0, 64-bit`
- **Database Name**: `eip_db`
- **Execution Date**: 2026-08-11

## Schema Verification
- **Tables Verified**: All 12 tables created successfully.
- **Constraints Verified**: Primary Keys, Foreign Keys, `UNIQUE`, `NOT NULL`, and `CHECK` constraints are actively enforcing data integrity.
- **Indexes Verified**: Created successfully and confirmed active via `EXPLAIN ANALYZE`.

## Seed Records
Seed execution successfully populated:
- `users`: 5 records
- `roles`: 3 records
- `permissions`: 6 records
- `categories`: 6 records
- `tags`: 10 records
- `documents`: 10 records
- Junction tables and `search_history` appropriately filled to reflect many-to-many relationships.

## Queries Executed
Both `common_queries.sql` and `reporting_queries.sql` successfully executed returning expected logical groupings, aggregations, and filtered records (e.g., proper document permission intersections).

## Tests Executed
The `schema_tests.sql` script correctly trapped intentional invalid states within `BEGIN; ... ROLLBACK;` blocks:
- **Test 1**: Caught UNIQUE violation on duplicate email.
- **Test 2**: Caught CHECK violation on bad document status.
- **Test 3**: Caught FOREIGN KEY violation on non-existent user mapping.
- **Test 4**: Verified `ON DELETE CASCADE` from Users to Search History.
- **Test 5**: Verified `ON DELETE SET NULL` from Users to Documents.
- **Test 6**: Caught UNIQUE violation on duplicate document versions.
- **Test 7**: Caught CHECK violation on invalid search type.

## Fixes Made
- Updated `schema_tests.sql` to explicitly use `BEGIN;` and `ROLLBACK;` transaction blocks. This isolates errors generated by intentional constraint violations so that subsequent tests continue to execute seamlessly.

**Conclusion**: The PostgreSQL schema is stable, fully 3NF compliant, and referentially sound.
