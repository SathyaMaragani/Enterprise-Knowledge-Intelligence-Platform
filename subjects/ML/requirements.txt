# Phase 1.7B-1 - dataset, preprocessing, baseline retrieval, evaluation
numpy>=2.0
pandas>=2.0
pyarrow>=15.0          # reading the BEIR parquet corpus
scikit-learn>=1.4      # TF-IDF baseline

# Phase 1.7B-2 - embedding models (not used yet)
# sentence-transformers>=3.0
# torch>=2.2

# Serving / notebooks (not used yet)
# fastapi
# uvicorn
# jupyter
